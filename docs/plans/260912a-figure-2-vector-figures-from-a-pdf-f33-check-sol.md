- **F35 · P1 · established guard bypass; wrong-crop consequence remains reasoned.** pdf.js does stop parsing with `stopAtErrors`, but `getOperatorList()` can resolve the already-flushed prefix before its rejection is observed. At a chunk boundary, forgiving parsing skips the malformed paint while strict parsing stops immediately after the same prefix, producing identical visible lists. An eligible probe page with 480 `q/Q` pairs followed by the test’s invalid `/Sh0 sh` returned 995 operators from both reads, set `strictAgrees: true`, and passed `locateDrawnFigure`. Thus the comparison at [pdf-figure-layout.ts:119](/home/greg/code/spideryarn2/.claude/worktrees/fb31-figure-2-image/src/pdf-figure-layout.ts:119) can still admit omitted paint to PDFium. Strict throws and aborts otherwise fail closed. The malformed test is genuinely recovered in forgiving mode; 62 focused tests passed, and the test failed against `7d3303ab`.

**F33 still open (F35).**