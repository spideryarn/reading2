F14 — closed: `journalRecords()` removes only the final split sentinel and preserves interior blanks (`tools/overseer/launch-admission.ts:49-61`), and both stores use it (`launch-admission.ts:405-407`, `launch-store.ts:115-117`); the blank-line regressions pass (`tests/overseer-launch-admission.test.ts:211-231`, `tests/overseer-launch-store.test.ts:171-176`).
F15 — closed: reset unions every valid occurrence id found under `launches/o/` into `carried` (`tools/overseer/launch-store.ts:215-241`), while planning refuses carried ids (`tools/overseer/launch-protocol.ts:1363-1365`); the artefact-only, released-slot regression passes (`tests/overseer-launch-protocol.test.ts:1101-1130`).
F16 — closed: `AttemptRef` and the `launching` event no longer carry an artefact path (`tools/overseer/launch-protocol.ts:287-299`, `:435-436`), reconciliation derives it through the open journal's `attemptDir` (`:1553-1557`, `:1611-1614`, `:1831-1833`), and the copied-journal/foreign-exit regression passes (`tests/overseer-launch-protocol.test.ts:1185-1200`).
F17 — closed: terminal records whose journal says the reservation is released now re-check owner truth and produce `release-untracked` when the key is reserved (`tools/overseer/launch-protocol.ts:1695-1705`, `:1708-1755`), which releases under the existing licence without adding a launch-journal line (`:1815-1821`); the repaired-owner resurrection regression passes (`tests/overseer-launch-protocol.test.ts:1237-1261`).
F19 — closed: the `other-boot` arm immediately records reboot completion using `identity.recorded` and `identity.current`, independent of the separate boot read (`tools/overseer/launch-protocol.ts:1643-1655`); the unreadable-boot regression passes (`tests/overseer-launch-protocol.test.ts:1203-1234`).
