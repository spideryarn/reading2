No P0: the scope cut is directionally right. The judge, generic arms framework, and full five-article live sweep are unnecessary for this prompt repair. I found six gaps before the cut is safe to build.

- **F58 — P1 — The plan contradicts its own stochasticity finding.** It correctly says evidence must be a rate over repetitions, then later replaces that with one replay of three known cases ([plan:426](/home/greg/code/spideryarn2/.claude/worktrees/critiques-mode/docs/plans/260906b-an-evaluation-for-debate-mode-and-what-it-finds.md:426), [plan:531](/home/greg/code/spideryarn2/.claude/worktrees/critiques-mode/docs/plans/260906b-an-evaluation-for-debate-mode-and-what-it-finds.md:531)). Those three cases are valuable regression anchors, but a single all-correct response can happen under the incumbent—as the journalled Skeptical Inquirer row proves. Use the three-row replay only as a cheap preflight. Before landing, retain the repeated incumbent-versus-repair comparison, with the incumbent required to reproduce at least one error; otherwise the experiment did not exercise the defect.

- **F59 — P1 — “33 replayable rows” mixes incompatible denominators and drops required context.** The journals contain 26 reported rows, but one has no exact matching annotation and production kept only 15; combined with the spike’s seven kept rows, the observed user-visible denominator is 22, not 33, making the observed displayed-error rate `3/22`, not `3/33`. More importantly, the database-only packets omit the evidence haystack even though the packet contract explicitly requires it ([plan:204](/home/greg/code/spideryarn2/.claude/worktrees/critiques-mode/docs/plans/260906b-an-evaluation-for-debate-mode-and-what-it-finds.md:204)) and the original judge design says the Geller case is invisible without the full extract ([plan:571](/home/greg/code/spideryarn2/.claude/worktrees/critiques-mode/docs/plans/260906b-an-evaluation-for-debate-mode-and-what-it-finds.md:571)). Treat the three source-quote-only cases as regression fixtures, not production-equivalent frozen packets; use annotation-backed, validation-kept rows for the rate.

- **F60 — P1 — D′ as currently described inherits F47 and adds selection and confirmation bias.** A human gold label is better than a model judge because it removes judge calibration error, but it still compares one categorical answer with another. It measures valence disagreement, not which target the model internally evaluated. The opposite-pair sampling frame is also supported by only three positives and cannot establish recall; matched controls do not yield a population rate without declared sampling probabilities and weighting. At this size, label every evaluable row. Present only the target, quote, and evidence to the labeler—hide the original `relation`, `valence`, and `applies`; label before repaired outputs exist; have a second person adjudicate the questionable rows and any arm disagreements.

- **F61 — P1 — The repetition design needs to be declared before any output is seen.** Use the production model and provider policy and send no `temperature` field, because production sends none ([src/debate.ts:1462](/home/greg/code/spideryarn2/.claude/worktrees/critiques-mode/src/debate.ts:1462)); forcing temperature zero would measure a different distribution and generation parameters have already caused provider refusal elsewhere. Run `N=10` repetitions per condition, interleaving incumbent and repair in randomized order. Score human-gold disagreement separately for regression anchors and held-out packets. Test the difference with a packet-stratified permutation test and report a 95% packet-clustered confidence interval plus per-packet rates. If inconclusive, extend both conditions to `N=20`; bringing the judge back would not reduce generation noise. At 26 packets, `N=10` means 520 individual outputs and is affordable at the repository’s model prices, but batch packets by article and pass with exact-ID coverage—roughly 120 calls—rather than making one HTTP call per packet.

- **F62 — P1 — P7 does not guard prompt vocabulary.** The existing test proves only that an out-of-vocabulary answer is silently converted to `unclear`/`unknown` ([test:1087](/home/greg/code/spideryarn2/.claude/worktrees/critiques-mode/tests/debate.test.ts:1087), [parser:772](/home/greg/code/spideryarn2/.claude/worktrees/critiques-mode/src/debate.ts:772)). It does not prove the repaired prompt emits the accepted vocabulary. With the live sweep removed, every repeated response must be checked before coercion: require zero out-of-vocabulary values, print raw vocabulary counts, and fail the run rather than crediting `unknown` as removal of a wrong label.

- **F63 — P1 — The dropped live sweep was the only check of prompt interactions with production search-and-generation.** Frozen packets prevent row selection by design, so they cannot detect the repaired shared `READING` paragraph changing which sources are selected, which quotations are emitted, or whether the live fenced answer remains parseable. The full judged sweep is excessive, but one non-evidentiary live smoke run through `generateDebate` is warranted before landing—preferably the cheap `writes` article. It should gate only integration, raw vocabulary, and non-catastrophic row production; it is not statistical evidence of the valence improvement.

- **F64 — P2 — B′ still carries most of the old eval despite the narrower question.** Gold-URL discovery, kept-per-returned, the full loss table, and Layer-0 packets P3–P6/P8 do not bear on a valence-only prompt repair. The necessary instrument is smaller: extract and pin eligible packets, enforce exact packet-ID coverage, preserve raw vocabulary, score against hand labels, and retain P7. The existing Layer-1 replay can remain reusable infrastructure without completing the broader scorer.

The factual premises otherwise hold: the raw response and annotation extracts are genuinely journalled; every inspected citation has non-empty `url`, `title`, and `content`. The count is not uniformly 11/12, however: the three runs are 11/12, 9/12, and 12/10. The prompt reading is correct: group two explicitly binds the claim, group one never explicitly binds “target,” neither rules out stance toward the source’s own subject, and `relation` and `valence` currently have different scopes.