The Stage 4 status is candid enough, but the trace reader is not yet safe to use for the CSS decision. Stage 5 is not build-ready as scoped: its inventory misses one surface, omits two focus-restoration defects it already identified, and defers the stage’s central keyboard defect.

| ID | Severity | What is wrong | What I would do |
|---|---|---|---|
| F34 | High | The shrink/pan verdict compares absolute maxima across the entire trace, including pinched samples, rather than comparable closed→open changes ([reader:210](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/scripts/read-viewport-trace.ts:210)). It can say “SHRANK; bottom-edge rule is right” when `bottomInset=100`, `offsetTop=50`, and the head is visibly clipped above. | Restrict classification to usable scale-1 transitions, compare deltas from a keyboard-closed baseline, and return `shrank`, `panned`, `mixed`, or `inconclusive`. Never endorse bottom-only arithmetic when the measured head is clipped. |
| F35 | High | The producer/reader seam has no runtime contract. The reader casts arbitrary JSON to its hand-copied type ([reader:384](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/scripts/read-viewport-trace.ts:384)); TypeScript therefore considers `win[1]`, `vv[2]`, and `band[3]` safe even when the file is malformed or from a newer probe. The copies have already drifted: `ev` and `vis` are weaker in the reader ([probe:97](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/ViewportProbe.tsx:97), [reader:58](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/scripts/read-viewport-trace.ts:58)). | Give the emitted format a version and validate tuple lengths, finite numbers, enums, required keys, and producer marker before analysis. A shared TS type is useful, but insufficient for stored external JSON. |
| F36 | High | Missing evidence can still produce a clean verdict. Missing head/composer rectangles become zero via `?? 0`; missing `visualViewport` produces a warning and can still print “No mobile defect” ([reader:180](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/scripts/read-viewport-trace.ts:180), [reader:230](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/scripts/read-viewport-trace.ts:230)). A trace in which the keyboard was never opened also receives a benign explanation. | Require a usable closed/open sequence, required geometry, and evidence that the keyboard interaction occurred. Otherwise print `INCONCLUSIVE` and exit nonzero. |
| F37 | Medium | The self-test does not exercise pan, hidden-above geometry, malformed data, or either `max`/`+` calculation ([reader:292](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/scripts/read-viewport-trace.ts:292)). An implementation that always says “shrank,” deletes the head-above path, or calculates `max` as addition can pass. It is also run by no repository gate. | Factor the analysis into importable pure functions and add Vitest cases for shrink, pan, mixed, malformed/incomplete traces, and exact arithmetic. Keep `--self-test` as a convenience if desired. |
| F38 | Medium | The probe’s `vis` field means “inside the layout viewport,” not “inside the visible strip”: it compares against `window.innerHeight` ([probe:136](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/ViewportProbe.tsx:136)). A dock behind the keyboard can therefore be reported as `on`. | Rename it to make the weaker meaning explicit, or calculate visibility against `visualViewport.offsetTop…offsetTop + height`. Do not print it as physical occlusion evidence unchanged. |
| F39 | Medium | Step 3½ overstates the 2026-09-04 report as an observation of the mechanism. It observed Send behind the keyboard on installed iOS ([report:19](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/user-feedback/260904_1723-mobile-keyboard-done-send-button.md:19)); the report explicitly says nobody measured a real phone’s viewport values ([report:111](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/user-feedback/260904_1723-mobile-keyboard-done-send-button.md:111)). The WebKit pan explanation remains an inference. | Say “a real-device symptom consistent with the mechanism.” The plan does correctly avoid promoting the band composer from hypothesis to defect. |
| F40 | Medium | Rejecting a CSS-characterisation test outright is wrong ([plan:590](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/plans/260906f-the-active-mode-gets-one-surface-and-one-way-to-fit-the-screen.md:590)). A real-browser test can inject a nonzero `--kb-inset` and distinguish the current rule, `max`, and addition. | Keep the iPhone trace as the decision evidence, but require a Chrome computed-geometry regression test once the rule is chosen. The repo already has a small Chrome-in-Vitest pattern ([example](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/tests/mark-sign-in-chrome.test.ts:15)). |
| F41 | High | The claimed complete sixteen-surface focus census is incomplete. `RefereeHowCard` is excluded because it is in flow ([inventory:35](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/plans/260906f-the-active-mode-gets-one-surface-and-one-way-to-fit-the-screen-focus-inventory.md:35)), yet its focused Close button is conditionally unmounted without restoration to “How this works” ([card:121](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/RefereeCard.tsx:121), [card:129](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/RefereeCard.tsx:129)). Being in flow removes the trap requirement, not the return-focus requirement. | Add it as surface 17, restore focus to the header toggle after its own Close button is activated, and test with the Close button focused before activation. |
| F42 | High | The inventory calls Chat and Annotate focus restoration real gaps ([inventory:106](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/plans/260906f-the-active-mode-gets-one-surface-and-one-way-to-fit-the-screen-focus-inventory.md:106)), but Stage 5 repairs neither. Annotate focuses its textarea and records no opener ([AnnotateDialog:182](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/AnnotateDialog.tsx:182)); Chat likewise has no restoration. | Capture and restore their openers, with a defined disconnected-opener fallback, and cover every close route. |
| F43 | High — decision | Deferring the hover-card product choice is sensible; deferring the defect and then completing Stage 5 is not. The plan itself says the cards are keyboard-openable, keyboard-unusable, and explicitly named by the acceptance checkbox ([plan:669](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/plans/260906f-the-active-mode-gets-one-surface-and-one-way-to-fit-the-screen.md:669)). | Either obtain the product decision and include the repair, or split out a preparatory Stage 5a and leave Stage 5/the checkbox explicitly incomplete. Calling the current scope done would be the F13 move. |
| F44 | Medium | Stage 5 treats Chat’s thread arm as one missing focus call, but the obvious `focusNonce={1}` fix contradicts the component’s recorded contract: opening an existing conversation must not steal focus into the composer ([ChatPanel:1864](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/ChatPanel.tsx:1864)). | Decide the target first—likely a dialog container, heading, or Close control—not the composer. Then test the declared behavior. |
| F45 | High | No retained test is planned for actual Tab/Shift-Tab order, despite that being the stage’s headline behavior. jsdom’s limitation is real, but the repository does have a Chrome-in-Vitest precedent. | Add a real-Chrome traversal check for representative native-modal and modeless behavior, and especially the eventual hover-card sequence. If that cannot be made reliable, leave the Tab checkbox explicitly incomplete and require recorded manual browser acceptance. |
| F46 | Medium | The Floating UI table says focus restoration is simply “yes.” Installed source makes it conditional: blank-space dismissal may return to the trigger, while focus that lands on another real control is respected ([Floating UI:2021](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/node_modules/@floating-ui/react/dist/floating-ui.react.mjs:2021)). | Mark it conditional and test both blank outside press and pressing another focusable control in Chrome. |
| F47 | Medium | Two nested-case claims are false or too strong. Multiple `showModal()` dialogs can stack—the code’s own comment says so ([Dock:1057](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/Dock.tsx:1057)—and CommandBar does not categorically restore “into the modeless dialog”; it restores the actual prior focus when still eligible ([inventory:128](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/plans/260906f-the-active-mode-gets-one-surface-and-one-way-to-fit-the-screen-focus-inventory.md:128)). | Correct the inventory and base the “cannot occur” conclusion on the app’s actual reachability guards, not a false platform rule. |
| F48 | Low | “Waiting on a requested trace” is honest, but “not the same as blocked” is semantically too absolute, and the nearby heading still says “blocked on a phone” ([status:53](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/plans/260906f-the-active-mode-gets-one-surface-and-one-way-to-fit-the-screen.md:53), [heading:89](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/plans/260906f-the-active-mode-gets-one-surface-and-one-way-to-fit-the-screen.md:89)). It remains externally blocked pending the requested response. | Use “Waiting on requested iPhone trace; A5 remains incomplete.” That preserves the useful next-action reframe without arguing terminology. |

I would refuse to rely on the first phone trace until F34–F36 are fixed, and I would not regard the instrument as adequately guarded until F37 is in a normal test gate. I would also refuse to build Stage 5 as one supposedly complete stage until F41–F45 are resolved in the plan. The mechanical backdrop, fixture, and outside-press work can proceed as a smaller preparatory stage.

No findings on these points:

- The `max` versus `+` wording handles an inconclusive trace correctly; “not disproved” is not an endorsement.
- Rejecting Chromium pinch as a substitute for Safari keyboard focus-pan is correct.
- The Stage 4 status does not launder completion: it explicitly leaves A5 incomplete and the fit undelivered.
- The backdrop-handler count, `reopen()` diagnosis, absence of UI `aria-modal`/`inert`, and the two Floating UI outside-press gaps are accurate.

Verification: the equivalent self-test invocation, `node --import tsx scripts/read-viewport-trace.ts --self-test`, passed with 2 traces and 4 assertions; the requested `npx tsx …` command was blocked here by the sandbox’s `/tmp` IPC restriction. Typechecking passed, as did the three targeted suites covering the dock’s modeless contract, title rename, and one-Escape ownership: 51 tests total.