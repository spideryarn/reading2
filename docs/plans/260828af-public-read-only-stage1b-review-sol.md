Not wrong in its bones. The owner/visitor split is the right design, and the visitor union genuinely has no comments/chat/glossary fields. But HEAD is blocked by three current failures.

1. **blocker — access survives a change of reader identity.** `useArticleAccess` keys its answer only by slug and depends on the boolean `signedIn`, not `user.id` ([App.tsx:338](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/App.tsx:338), [App.tsx:351](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/App.tsx:351)). Owner A → user B leaves A’s private article and `OwnedReader` mounted indefinitely because both are signed in. Owner → signed out exposes it for the render before the effect clears state. Pass the reader id/null through `ArticlePage`; tag cached access with `{slug, readerId}` and synchronously return loading unless both match. Test identity changes in the same mounted root.

2. **blocker — hovering an article link leaves `/api/public/`.** Every visitor mounts `ProseHoverCard` ([App.tsx:1911](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/App.tsx:1911)), which mounts `useLinkFacts` ([ProseHoverCard.tsx:194](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/ProseHoverCard.tsx:194)). Opening an external-link card requests authenticated `/api/library`, and Wikipedia for matching links ([link-facts.ts:177](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/link-facts.ts:177), [link-facts.ts:339](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/link-facts.ts:339)). This is the existing in-article hover path, not stage-2 link unfurling. Split the asynchronous facts hook into an owner-only wrapper; visitors should receive locally derived link details only. Add a linked block and hover/focus interaction to the trace.

3. **blocker — a failed publication can be reported as unchanged.** The catch correctly admits that a write may have committed before its response was lost ([AccessSharing.tsx:145](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/AccessSharing.tsx:145)), then the unknown branch says, “Whatever it was before is unchanged” ([AccessSharing.tsx:166](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/AccessSharing.tsx:166)). After a successful publish with a lost response, that tells the owner a public article remains private. A slow publish also continues drawing `SHARING_OFF` until the response arrives. Model known/pending/unknown explicitly and say that an uncertain write may have taken effect. A malformed successful body also currently collapses to private because the unchecked cast at line 142 reaches `visibility === "public"` at line 99. Validate the response at runtime; invalid 2xx responses become unknown. Test deferred writes, failed publish and unpublish, 404, 501, malformed 200/204, and double-click.

4. **should-fix — a failed metadata check hides Access & Sharing.** Failure leaves `provenance` null, which makes `hasShelfRow` false and removes the section entirely ([Metadata.tsx:296](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/Metadata.tsx:296), [Metadata.tsx:413](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/Metadata.tsx:413), [Metadata.tsx:878](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/Metadata.tsx:878)). Render the unknown card—with no control—when metadata fails. Hide it only for a known fixture.

5. **should-fix — the rename seam is still a convention.** Visitors unconditionally mount the authenticated-write `useArticleRename` hook; `offer={false}` merely makes its trigger unreachable today ([Masthead.tsx:91](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/Masthead.tsx:91), [Masthead.tsx:146](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/Masthead.tsx:146)). Split owner/visitor mastheads or build the rename capability in an owner-only component. The named comments/chat/glossary hooks, job consumers, record-open POST, and `AnnotateDialog` are otherwise structurally owner-gated in steady state.

6. **should-fix — the four visitor sentences are not truthful states.**

   - Failed metadata becomes `not-yet-public`, whose copy asserts “There is…” ([App.tsx:423](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/App.tsx:423), [visitor.ts:97](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/visitor.ts:97), [messages.ts:1125](/Users/greg/Dropbox/dev/experim/spideryarn2/src/messages.ts:1125)). That is a fifth state: availability unknown.
   - `/tweets` always uses `TWEETS_GAP`, ignoring `available.tweets`, so it claims a thread exists even when the wire says false ([App.tsx:731](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/App.tsx:731), [visitor.ts:112](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/visitor.ts:112)).
   - A signed-in non-owner is told “Chat is for signed-in readers,” although signing in leaves them on the visitor path ([messages.ts:1142](/Users/greg/Dropbox/dev/experim/spideryarn2/src/messages.ts:1142)). The account pitch also promises this article’s chat/glossary/shelf before the save/fork stage exists.

   Add an availability-unknown gap, derive tweets from its flag, and use ownership-neutral wording for unavailable tools.

7. **should-fix — the network acceptance test misses whole public paths and interaction work.** It opens only `/read/:slug` with query modes, never `/metadata` or `/tweets` ([public-network-trace.test.tsx:240](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/public-network-trace.test.tsx:240), [public-network-trace.test.tsx:281](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/public-network-trace.test.tsx:281)). Its signed-in visitor case checks copy, not requests ([public-network-trace.test.tsx:325](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/public-network-trace.test.tsx:325)). Sweep all three views for signed-out and signed-in visitors, change modes by clicking controls, hover/focus an article link, advance timers, and assert that the signed-in case has only the intentional owned-article probe outside the public namespace.

8. **should-fix — concrete mutations each scoped test file currently misses.**

   - `public-network-trace`: mount private `Metadata`/`Tweets`, retain access across an account switch, or fetch from a hover/click/timer.
   - `public-client-fetch`: change the loaders to bare `fetch(path)`; credentials are checked only by calling `publicFetch` directly ([public-client-fetch.test.ts:45](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/public-client-fetch.test.ts:45)).
   - `access-sharing`: retain today’s “unchanged” failure claim, remove busy protection, or mishandle failed publication only; it tests only failed unpublishing ([access-sharing.test.tsx:313](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/access-sharing.test.tsx:313)).
   - `visitor-gaps`: map `ideas.has` to `summary`; its fixtures correlate all flags and directly vary only glossary ([visitor-gaps.test.ts:36](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/visitor-gaps.test.ts:36)).
   - `metadata-sharing-card`: discard `personalised` while wiring the prop, or return metadata 500; neither is asserted ([metadata-sharing-card.test.tsx:109](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/metadata-sharing-card.test.tsx:109)).
   - `client-imports`: add `import.meta.glob("../*.ts")`; the regex scanner cannot see it ([client-imports.test.ts:137](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/client-imports.test.ts:137)). Detect Vite glob imports or inspect the built module graph.

9. **consider — `publicFetch` checks the unnormalised string.** `/api/public/../article/x` and `/api/public/..\\article/x` pass `startsWith` but browsers normalise both to `/api/article/x` ([public-api.ts:59](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/public-api.ts:59)). Current loaders encode their slug and are safe, but the exported guard is weaker than it claims. Validate the resolved same-origin URL and its normalised pathname.

No files were changed.