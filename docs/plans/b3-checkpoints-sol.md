NO-SHIP

1. [Verified] [checkpoints-fs.ts:157](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/checkpoints-fs.ts:157), [checkpoints-pg.ts:108](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/checkpoints-pg.ts:108) — an unreadable or caller-invalid entry can never heal. `read` treats it as a miss, the caller buys the work again, then first-write-wins preserves the bad entry and merely refreshes its age. Every retry can therefore repay forever. The current test only checks the initial miss at [store-checkpoints.test.ts:256](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/store-checkpoints.test.ts:256). Smallest fix: make writes atomically replace existing values in both adapters, or add a concurrency-safe invalidation mechanism; test corrupt/invalid → one repurchase → next retry hits.

2. [Verified] [checkpoints-fs.ts:152](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/checkpoints-fs.ts:152) — `write(..., undefined)` successfully writes `undefined\n`, while Postgres rejects it at [checkpoints-pg.ts:99](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/checkpoints-pg.ts:99). This violates adapter parity and the interface’s promise to reject unserialisable values. Combined with finding 1, it creates a permanent paid miss. Smallest fix: serialize first, reject when `JSON.stringify` returns `undefined`, then write; add a shared adapter test.

3. [Verified] [checkpoints-fs.ts:87](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/checkpoints-fs.ts:87), [checkpoints-fs.ts:191](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/checkpoints-fs.ts:191) — filesystem failures are broadly swallowed. `EACCES`/`EIO` become cache misses, and sweep failures become a successful zero report. The silent `utimes` catches also defeat observability. Smallest fix: treat only `ENOENT` as absence; propagate other read/sweep errors, and log sanitized warnings for best-effort timestamp or cleanup failures.

4. [Verified] [store-checkpoints.test.ts:313](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/store-checkpoints.test.ts:313), [delete-the-importer.md:924](/Users/greg/Dropbox/dev/experim/spideryarn2/docs/plans/delete-the-importer.md:924) — the empty-key test does not prove “asks nothing”: deleting either adapter’s early return still passes. The plan also claims all 25 tests were mutation-tested, then lists fewer exceptions than the test header. Smallest fix: instrument query/I/O calls or narrow the test’s claim, and correct the evidence record.

5. [Conditional suspicion] [checkpoints.ts:125](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/checkpoints.ts:125) — the regex is safe for the stated macOS/POSIX concerns and component length, but accepts Windows reserved basenames such as `con`, `nul`, and `com1`; `con.json` is still reserved. If Windows is supported, reject those names or tighten the contract to the actual 16-character hex keys. I did not execute this on Windows.

Trace: I inspected the implementations, migration/schema, tests and mutation claims, both real key minters, the B3 plan, database/security docs, `owner.ts`, `ownedSlug`, and the job/draft resolver. I found no current cross-owner path: slug is globally unique, articles have one non-null owner, and the intended draft reference is owner-filtered. Stage D is not built, so I could not verify that it binds only that trusted reference or wraps the Postgres store with `guardDbStore`.

`UPDATE … RETURNING` for Postgres reads is reasonable: concurrent reads serialize, and a timestamp failure throws before the caller buys work. The filesystem should still return a hit after a timestamp failure, but log it. The closed namespace, article-scoped primary key, manual retention decision, and duplicated real-key regex tests are defensible; both key tests are non-vacuous. The duplicate-PDF cost is stated in the plan.

I did not rerun the supplied full gate or mutation campaign. A read-only live database probe was blocked by the execution sandbox, so database execution evidence remains the supplied gate results rather than independently reproduced output.

