NO-SHIP

1. **High — verified: nothing tests the successful `parts` write path.** Every session test supplies incomplete, empty, or absent `parts`; no test commits a complete product. Deleting the `store.write(...)` call at [session.ts:156](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/session.ts:156) leaves all eight session tests green. The real-blocks test also stays green because `blocks.run` still writes both files itself and returns no `parts`. The entire runner could revert to its old direct `assertProduced`/`finishStep` path while preserving that test’s observable results. Deleting `setAttribute("id", …)` correctly reddens the artefact assertions, but proves the blocks stage—not the new commit write.

   Smallest correction: add a successful converted-product test that starts over old artefacts, commits complete new `tree` and `labels` parts, and asserts one write, replacement bytes, completion, and marker removal. Also inject or mock a session in the job test so bypassing `session.commit` fails. Assert the real step’s `detail`, which would also catch an old string return being accepted as an unconverted product.

2. **High — verified: D1a has not modeled the atomic boundary D1b needs.** `commit` ends at `finishStep` [session.ts:154](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/session.ts:154). Only afterwards does `runStep` mark the job step done [jobs.ts:413](/Users/greg/Dropbox/dev/experim/spideryarn2/src/jobs.ts:413), and `advanceJob` later releases or finishes the job in separate calls [jobs.ts:876](/Users/greg/Dropbox/dev/experim/spideryarn2/src/jobs.ts:876). An all-skipped claim never calls `commit` at all. Therefore D1b cannot merely replace the filesystem adapter: it must change session construction, status ordering, and the success API to include the job transition/publication.

   Building one stateless filesystem session per step is behaviorally harmless today, but it does not establish the required lifetime. Move construction to immediately after the successful claim and pass the session into `runStep`. Give `commit` an explicit job-transition payload—or an equivalent session operation—including the all-skipped terminal path.

3. **Medium — verified: `UNCONVERTED_STEPS` is fail-open for future conversions.** If a converted stage is accidentally left in the set and later returns no `parts`, `checkProduct` permits it and `assertProduced` can succeed against old filesystem artefacts. The equality test at [store-session.test.ts:252](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/store-session.test.ts:252) cannot detect that conversion drift; it only makes `UNCONVERTED_STEPS` and `STEP_ORDER` agree. Worse, it requires every newly added step to receive the unsafe exemption.

   Production does consult the correct step: both `run` and `commit` use `STEPS[step.name]`. The correction is to make new steps converted by default and statically require `parts` for names outside the legacy set, while testing only that legacy names are valid pipeline steps—not that every pipeline step is legacy.

4. **Medium — verified edge cases in `checkProduct`.** For ordinary object literals with non-empty `produces`, the original hole is closed: absent `parts`, `{}`, missing keys, and keys explicitly valued `undefined` are rejected before `store.write`; carried artefacts cannot satisfy that check. The carried-output test is good evidence for ordering.

   Two boundaries remain:

   - `parts[kind]` accepts inherited values. `Object.entries(parts)` then writes none of them, allowing carried artefacts to satisfy `assertProduced`. Require `Object.hasOwn(parts, kind)` as well as a defined value.
   - Extra keys are not rejected before writing. The filesystem store can write earlier valid entries and then throw on an invalid `(step, kind)` pair. Validate that every own key belongs to `step.produces` before writing.
   - A converted step with `produces: []` and `parts: {}` is accepted and finished, although filesystem `has([], …)` deliberately returns false [artifacts-fs.ts:431](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/artifacts-fs.ts:431), so it can never subsequently be considered done. Prohibit empty `produces` until it has explicit completion semantics.

`ArtifactReads` itself accurately contains the six operations used across preflight and stage execution: three preflight methods and three stage-facing methods. No stage calls `store.write` through that parameter. All nine stages still perform direct filesystem writes, deliberately. The narrowing prevents an ordinary `.write` call at compile time, but it is not a runtime capability boundary: [session.ts:152](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/session.ts:152) exposes the full store object as `reads`, so an assertion can widen it back. D1b should return a six-method facade rather than the mutable object.

Trace: I read the D1 design review, the accepted plan changes and D1a/D1b split, `silent-success.md`, the session/store contracts and adapters, the runner claim/release paths, all eight session tests, and the real-blocks job test. I ran `npx vitest run tests/store-session.test.ts` (8 passed), the filtered real-blocks job test (1 passed), scoped Biome lint, `git diff --check`, and direct `tsc --noEmit`. The normal typecheck wrapper could not create its IPC socket in this sandbox; direct `tsc` reported six concurrent assets-pipeline errors, which I excluded. I ran no Postgres tests or database-writing command. I could not independently verify the reported ten mutation runs because no mutation results were supplied. The provided scratchpad diff disappeared after its initial read, so I reconstructed D1a from the named files and current diff while excluding the live assets work. No source file was changed; the targeted tests created and cleaned only their own fixtures.