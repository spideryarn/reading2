# Review: realtime chat repair plan

Read-only: do not change any file. Review `docs/plans/260906f-repair-realtime-chat.md` before implementation. Repo is this worktree, branch `worktree-realtime-chat-repair`, base `28096583db9a5cbee322e0f0936e54aeaaea86eb`. Untracked candidate: that plan; no implementation yet. Read source where needed; start with `src/web/live/useLiveConversation.ts`, `src/web/ChatPanel.tsx`, `src/web/mic-devices.ts`, `src/web/live/exchanges.ts`. The user wants native two-way article conversation, starting/resuming, hangup to typing/dictation, capability first, visible mic feedback/transcript, tools and useful fallback.

Attack the design independently. Identify concrete scenarios the plan fails to handle and the smallest adjustment. P0=data loss/security/incorrect charging/broad outage; P1=user-visible incorrect behavior or authoritative contract violation; P2=maintainability risk without current wrong behavior; P3=prose. Give stable F1 etc IDs, established/reasoned, evidence and remedy. Refuse only established P0/P1. Say READY or NOT READY explicitly. You may run one local-only test with `node node_modules/vitest/vitest.mjs run tests/live-session-flow.test.tsx`; no network/database available. Do not run broad suites. Existing tests are evidence of their scope, not proof of real audio.

Known facts, read after your independent pass: local provider token mint succeeds; missing chat error/transcript surface, ignored chosen mic, missing level meter, no playback rejection handling, startup timeout and failed phase defects independently traced. Simpler UI-only repair rejected because these defects are reachable. Keep existing persistence and WebRTC; no new abstraction or schema by default.
