Do not ship this exact version. The guard closes the original append-to-existing finding, but exposes a separate High race.

- **High — sending before the initial fetch completes can erase the new conversation.** The composer renders whenever `threadId === null`, even while `loaded === false` ([ChatPanel.tsx:391](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/ChatPanel.tsx:391)). `send(null, …)` correctly mints and inserts optimistic rows, but the outstanding initial refresh later replaces the entire list with its older snapshot ([useChat.ts:543](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/useChat.ts:543)). Subsequent stream patches cannot recreate the removed thread ([useChat.ts:579](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/useChat.ts:579)). The reader then sees the list with a non-null URL, no composer, and their submitted question missing. This is exactly a reachable “non-null, list showing, reader lost something” state.

- **Medium — the box genuinely appears and disappears for a new reader.** With no stored conversations, it appears during loading. Once loading finishes, the auto-start effect creates an empty conversation and unmounts it ([App.tsx:1745](/Users/greg/Dropbox/dev/experim/spideryarn2/src/web/App.tsx:1745)). A half-typed draft survives in the ref, but vanishes from view until the reader closes that automatically opened conversation. The test asserting the box exists with no conversations ([chat-list-composer.test.tsx:152](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/chat-list-composer.test.tsx:152)) currently blesses this transient, unsafe state.

- **Low — the regression fixture uses an impossible URL id.** `"t1"` ([chat-list-composer.test.tsx:43](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/chat-list-composer.test.tsx:43)) is rejected by `threadParam`; real ids are `spya-…`. It does test `ChatPanel`’s non-null branch, but use a valid id so the test’s URL claim is true.

On the original finding specifically: yes, the guard works. In a committed render, `threadId={thread}` and `onSend` close over the same `thread`; therefore a visible composer means `send(null, …)`. The loading-with-a-named-thread and permanently stale-id cases merely lose the shortcut; `+` still opens a composer. The list draft ref also survives guard-driven unmounts, although an exact `null → stale non-null → null` draft test would be worthwhile.

This need not wait for `App.tsx`. A safe panel-only stopgap is to offer the composer only when `threadId === null && threads.length > 0`. Empty accounts already auto-start a conversation; after an explicit discard, `+` remains. Alternatively, fix initial refresh so it cannot overwrite local work. `onSendNew` alone would not fix this separate race.

Verification: the 9 new tests and 11 existing `useChat` recovery tests pass individually; Biome reports no issues in the changed panel/test files.