The trace-reader refusal condition is **not met**: F34 moved into a temporal-order bug, and F35–F36 still allow unsupported answers.

The Stage 5 refusal condition is **met in the narrow sense**: it is genuinely split, and 5a cannot complete the checkbox. But the review log’s stronger claim that F41–F45 are all resolved is wrong: F44 remains undecided, and F45 is not attached to 5b.

| Severity | What is wrong | What I would do |
|---|---|---|
| **Blocker** | **F34 still has a real bug.** [`classify()`](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/scripts/viewport-trace.ts:303) finds a closed sample anywhere, then classifies every moved sample—including samples before that baseline—as open. I ran an open→closed-only trace; it reported `SHRANK`, `bottomRuleSuffices: true`, and `DEFECT`. This is realistic because Clear removes every sample without recording a new start ([ViewportProbe.tsx](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/ViewportProbe.tsx:467)). Clear while the keyboard is open, then close it, and the future closed frame becomes the supposed baseline. | Process samples chronologically. A baseline must precede the open samples it governs. Divide the trace into closed→open→closed cycles; report cycles separately or return `mixed` when their settled results disagree. Split/refuse at rotations or layout-size changes. Use the two manual `mark`s for the settled decision and report animation frames separately. |
| **High** | **Transient and settled motion are conflated.** One frame where both deltas exceed 1px makes the whole trace `mixed`, even if `offsetTop` later falls to zero and the marked settled state is a pure shrink. Conversely, multiple cycles and rotations are pooled into one bag. That describes “something happened sometime,” not necessarily the mechanism whose CSS is being chosen. | Produce two results: marked closed→settled-open motion, and transient motion/clipping during animation. Retain the “any clipped head” warning, but do not let an unlabelled transitional frame silently become the settled arithmetic decision. |
| **High** | **F35’s claimed runtime contract is incomplete.** Validation accepts any event string, ignores the producer marker/version/count, discards `of`, and treats `dm`, `bars`, and `vis` as optional ([validation](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/scripts/viewport-trace.ts:108)). I supplied a trace with no `head` and `ev: "banana"`; it was accepted and returned `clean`. More seriously, rejected samples are printed but removed before classification, so a malformed clipped frame can be dropped while the remaining subset exits 0 as clean. | Validate the deployed marker and `head.count`, event/display enums, timestamp order, and the fields used as keyboard evidence. Accept the deployed format as explicit legacy v0 and add a version to future probes. Any rejected sample should make the arithmetic decision inconclusive unless parsing becomes field-tolerant enough to prove the rejected field was irrelevant. |
| **High** | **F36 still does not prove that a keyboard interaction occurred.** “Open” currently means only that visual-viewport numbers moved. Mobile browser chrome or another scale-1 viewport change can satisfy that. The probe records the focused element in `rect.focus` and `of.focus` ([ViewportProbe.tsx](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/ViewportProbe.tsx:277)), but the reader discards `of` and never requires focus evidence. | Require a chronological marked transition plus an editor/composer focused during the open endpoint. If the trace lacks that evidence, say the viewport moved but the keyboard experiment was not established. |
| **High** | **`MUST_REACH = ["head", "composer"]` is the wrong global contract** ([viewport-trace.ts](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/scripts/viewport-trace.ts:291)). For the specifically requested Chat trace, a missing composer should be inconclusive—it means the requested experiment was not captured. For a generic Glossary/Outline trace, it is a bug: those legitimately lack a composer, and `ModeSurface` also permits no head. The current check is additionally too weak in the opposite direction: a rectangle seen only in the closed baseline counts as sufficient evidence for the open state. | Give the analysis an expected surface profile. The Chat profile requires band, head, composer and focused editor in the settled-open sample; a non-composer profile does not. Require expected rectangles in each decision-bearing open sample, not merely somewhere in the trace. |
| **High** | **The strongest third uncaught mutation is changing the CLI’s inconclusive return to `0`.** All 23 tests still pass because they import only the pure analysis file; none exercises [`read-viewport-trace.ts`](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/scripts/read-viewport-trace.ts:170). There is already a contract mismatch: a missing/unreadable file rejects `main()` and exits **1**, although the stated contract says unreadable is **2** ([rejection handler](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/scripts/read-viewport-trace.ts:173)). | Add subprocess tests for clean/defect = 0, inconclusive = 3, invalid JSON/no samples/missing file = 2, and no argument = 2. Catch `readFile` failure inside `main()` and return 2. This is the mutation I would watch red next. A smaller additional mutation worth pinning is `SLOP = 0`; every current fixture uses integer movement. |
| **Medium** | **The three-surface focus generalisation is false, not merely unargued.** Annotate and Chat deliberately move focus on their draft arms. `RefereeHowCard` does not: opening it leaves focus on the always-mounted toggle; only a reader who Tabs into the card and activates its Close button loses focus when that button unmounts ([RefereeCard.tsx](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/RefereeCard.tsx:121)). Chat also loses focused content when draft becomes thread, before the dialog itself closes. The review log’s statement that all three “take focus on open” is wrong. | Keep steps 4 and 5 separate. Annotate/Chat need captured dynamic openers, meaningful fallbacks, and every close route covered. Referee has a known permanent destination: hold a ref to `RefereeHowButton` and restore directly to it when the card closes. Rewrite the inventory/review-log “one shape, one fix” claim. |
| **High** | **F44 was recorded as accepted but was not actually decided.** Stage 5a step 6 still says to choose among container, heading, or Close control ([plan](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/plans/260906f-the-active-mode-gets-one-surface-and-one-way-to-fit-the-screen.md:738)). That is the original decision moved into an implementation imperative. | Choose the Close control in the plan: it exists immediately in every thread/loading/gone state, is already a genuine tab stop and escape route, and matches `CommentDialog` without violating the “do not focus the composer” contract. |
| **High** | **F45 is only half disposed.** Stage 5a now requires representative native/modeless Chrome traversal, which is good. But Stage 5b—the actual keyboard-unreachable hover-card defect—contains no retained traversal-test requirement ([Stage 5b](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/plans/260906f-the-active-mode-gets-one-surface-and-one-way-to-fit-the-screen.md:753)). | Add a 5b acceptance step in real Chrome covering keyboard-open → Tab into the card → traversal of its controls → Shift-Tab/exit → dismissal and focus return under whichever product option Greg chooses. Keep the honest manual fallback. |

The F38 choice is sound for the trace that is about to arrive: computing visible-strip occlusion in the backwards-compatible reader is sufficient, and changing the deployed producer cannot alter an already-produced trace. I would still correct or rename `vis` on the next deployment so future raw traces do not carry a knowingly misleading “on screen” field, but that is not a refusal condition.

The other dispositions look genuinely carried through: F39, F40, F41’s census inclusion, F43, and F46–F48. F37’s move into the ordinary gate is real, but its exit-code half remains unguarded.

Verification: the targeted suite passed through `npm test`—23/23. Full typechecking passed using the non-IPC invocation; the ordinary `npm run typecheck` command itself hit the sandbox’s `tsx` socket restriction, not a TypeScript failure.