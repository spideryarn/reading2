NOT READY. No P0s; three established P1 gaps need small plan amendments.

- **F1 — P1 — Established: Live remains reachable from Remember.** The plan does not explicitly restrict the repaired controls to Chat. Today `live` is passed to every open conversation regardless of kind ([ChatPanel.tsx](/Users/greg/dev/spideryarn/reading2/.claude/worktrees/realtime-chat-repair/src/web/ChatPanel.tsx:493)), and the composer renders it unconditionally when supplied ([ChatPanel.tsx](/Users/greg/dev/spideryarn/reading2/.claude/worktrees/realtime-chat-repair/src/web/ChatPanel.tsx:2088)). This violates the authoritative statement that Live outside Chat—including Remember—is not built ([live-conversation.md](/Users/greg/dev/spideryarn/reading2/.claude/worktrees/realtime-chat-repair/docs/project/live-conversation.md:299)); spoken turns also have no Remember stance ([chat.ts](/Users/greg/dev/spideryarn/reading2/.claude/worktrees/realtime-chat-repair/src/chat.ts:453)).  
  **Remedy:** State that Live is rendered and startable only when `open.kind === "chat"` and that new spoken conversations are created only in Chat mode. Add a negative Remember test.

- **F2 — P1 — Established: rendering `live.lines` creates duplicate turns while persistence is pending.** `speak()` synchronously registers provisional chat rows ([useChat.ts](/Users/greg/dev/spideryarn/reading2/.claude/worktrees/realtime-chat-repair/src/web/useChat.ts:461), [controller.ts](/Users/greg/dev/spideryarn/reading2/.claude/worktrees/realtime-chat-repair/src/web/chat/controller.ts:268)), but the hook removes the corresponding live lines only after the server append resolves ([useLiveConversation.ts](/Users/greg/dev/spideryarn/reading2/.claude/worktrees/realtime-chat-repair/src/web/live/useLiveConversation.ts:476), [useLiveConversation.ts](/Users/greg/dev/spideryarn/reading2/.claude/worktrees/realtime-chat-repair/src/web/live/useLiveConversation.ts:517)). Once the plan renders live transcript by default, every completed exchange appears twice for the duration of the request.  
  **Remedy:** Define the ownership handoff explicitly: suppress the live copy as soon as `speak` registers its provisional rows, restoring it if the append fails. Test exactly one visible copy before, during, and after a deferred append.

- **F3 — P1 — Established: an old seed deadline can terminate a resumed session.** The timeout is local to each start but consults the shared `seeding.current` and invokes the current global stop ([useLiveConversation.ts](/Users/greg/dev/spideryarn/reading2/.claude/worktrees/realtime-chat-repair/src/web/live/useLiveConversation.ts:1279)). Hangup advances the epoch but does not clear that timer ([useLiveConversation.ts](/Users/greg/dev/spideryarn/reading2/.claude/worktrees/realtime-chat-repair/src/web/live/useLiveConversation.ts:856)). Start A, stop it while seeding, then start B: A’s deadline can observe B still seeding and kill B. “Bound/cancel startup” is too broad to preserve this invariant reliably.  
  **Remedy:** Require every startup deadline to be epoch-owned and cleared on every success, failure, stop, and replacement. Add the A-stop-B-start-old-deadline scenario explicitly.

The existing WebRTC, persistence, tool, and fallback direction is otherwise sound.

The permitted targeted test did not execute: Vitest failed before discovery with `EPERM` creating its macOS temporary directory, reporting `0 test`. No broad suites were run. Two test files also became modified concurrently during the review; I treated them as concurrent work, not implementation evidence.