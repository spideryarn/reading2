# GPT Sol code review, stage 1 — FAQ mode (260916d)

You are reviewing AND fixing stage 1 of a planned change. You have write access to this worktree.

## The candidate

- Commit `b31d8b87` (one commit, parent `03054917`). Diff: `git show b31d8b87`. Changed paths:
  `git show --name-only --format= b31d8b87`. Start with `src/faq.ts`, `tests/faq.test.ts`,
  `src/pipeline.ts` (the `faq` step and `FORCE_ONLY_WHEN_NAMED`), `src/store/pg.ts`,
  `src/routes.ts` (`GET /api/faq/:slug`), the migration `drizzle/20260916150539_faq.sql`. That list
  does not limit scope.
- The plan it implements: `docs/plans/260916d-faq-mode.md` (§ What v1 is, § Stages 1, and the
  ledger of your own plan review, `docs/plans/260916d-faq-mode-review-sol.md`).
- The precedents it copies: `src/ideas.ts` (stage shape) and Citations stage 1, commit `85631f9b`
  (the file list; the `"spaced"` verification in `src/citations.ts`).

**Another agent is concurrently building stage 2 (the client) in this same worktree** — uncommitted
changes to `src/modes.ts`, `src/web/**` (other than `src/web/lib/api.ts` and `src/web/Metadata.tsx`),
`src/mode-catalog.ts`, `src/title-text.ts`, `src/messages.ts`, docs, and many tests may appear while
you work. They are not yours to review or touch. Restrict your edits to files in the stage-1 commit.

## Evidence already run (you have no network or loopback, so Postgres tests are mine to run)

`docs/plans/260916d-faq-mode-stage1-test-results.txt` — 15 scoped files, 687 tests, exit 0, on
`b31d8b87`. `npm run typecheck` exited 0 on the same commit. Pure tests you may run yourself with
`node --import tsx node_modules/vitest/vitest.mjs run tests/faq.test.ts` or similar, if your sandbox
allows.

## What to do

1. **An independent pass first.** Hunt for wrong behaviour: a passage that could be stored that is
   not the article's characters; a verification path that accepts a quote in the wrong block; the
   stamp and the stage disagreeing on the fingerprint (null meta, tree, body-evidence filtering); an
   empty/all-dropped outcome that records success; the cap/dedupe/order rules in the plan not
   holding; the budget undersized; the step recording, staleness (`stale`/`outdated`) or the GET
   route's contract differing from the Ideas/Citations precedent; a store read/write/export/carry
   path missing; the CHECK constraint; the prompt (`FAQ_SYSTEM`) contradicting the plan's product
   definition or inviting summary-shaped rows.
2. **Write your findings to `docs/plans/260916d-faq-mode-code-review-1-findings.md` FIRST**, before
   fixing anything: an ID per finding (C1, C2, …), severity, evidence (file:line), consequence, fix.
3. **Then fix what is inside this stage**, narrowly and red-first where a test can show it (write
   the failing test, then the fix). Report, do not fix, anything wider. Do not commit.
4. Your final answer: the findings list with, for each, *fixed* (which files) / *reported only* /
   *not a defect after all*, and anything you could not verify because it needs Postgres.

## Severity scale

| | |
|---|---|
| **P0** | data loss, exploitable security, incorrect charging, or the service broadly unusable |
| **P1** | user-visible wrong behaviour, or an authoritative contract violated |
| **P2** | design or maintainability risk with no wrong behaviour today |
| **P3** | non-behavioural prose or comment defect |

## My own suspicions (already mine, worth less — spend most of the run elsewhere)

- `MAX_QUESTIONS` is applied to the model's order before reading order; is that the right cut?
- `FaqDropped.overCap` counts both extra passages and extra questions — one counter, two facts.
- `STEP_BUDGET_MS` is 150s against measured runs of 29–35s.
- The migration was generated by `drizzle-kit generate` and did include the CHECK DROP/ADD, contrary
  to the plan's expectation; check it matches `STEP_ORDER` exactly.
