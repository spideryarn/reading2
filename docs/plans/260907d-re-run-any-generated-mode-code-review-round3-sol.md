No F17 findings. F12, F13, and F14 all hold; I would accept the candidate.

- **F12:** Stage 3½ §1 explicitly prescribes `$0.20–0.40` for a completed short-article run, rising with length. The implementation performs two sequential web-search model calls for successful completion.
- **F13:** Yes receives focus after the confirmation mounts and its `aria-describedby` resolves to the price sentence. Cancel remains the next normal focus target; there is no focus trap or modal behavior.
- **F14:** Derived `asking` immediately yields to any queued/running job, while the effect permanently clears the obsolete confirmation. A vanished Retry callback also invalidates its confirmation. Excluding `starting` preserves the confirmation during its own POST.

Verification: `npx vitest run tests/metadata-rerun-section.test.tsx` — **15 passed, 0 failed**. No files changed.