## Findings

1. **Blocker — the client does not build.** [styles.css:13589](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/styles.css:13589) ends with an unmatched `}`. Both PostCSS and `npm run build:client` reject it; Vite reports `CssSyntaxError: Missing opening {`. Delete the final brace.

2. **Blocker — typechecking fails.** [FeaturesPage.tsx:42](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/FeaturesPage.tsx:42) still imports `Feature` after every feature row became a `Tile`. `tsc` reports TS6133. Remove the import.

3. **High — “Sign in” is a dead link on `/features`.** [SiteBits.tsx:92](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/SiteBits.tsx:92) always links to `#sign-in`, but only the landing page defines that id. On `/features`, clicking it does nothing. Use `here === "home" ? "#sign-in" : "/#sign-in"` or render a sign-in target on both pages.

4. **High — the landing nav overflows at the standard 320px reflow width.** [SiteBits.tsx:62](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/SiteBits.tsx:62) makes both halves non-shrinking and non-wrapping. With the actual Geist metrics, the landing variant needs about 331px: 143px wordmark + 24px outer gap + 56px Features + 16px inner gap + 44px Sign in + 48px padding. At 320px, the page scrolls sideways. Use 1rem nav padding below `sm`, reduce the outer gap, or hide another label at that width.

5. **High — the diagram copy is wrong in both places.** [LandingPage.tsx:240](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/LandingPage.tsx:240) says there are four diagrams; [FeaturesPage.tsx:206](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/FeaturesPage.tsx:206) says force has three others. The code has five: `force`, `drift`, `trail`, `sketch`, and `illustrated` at [diagram.ts:140](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/diagram.ts:140). Say “four maps … and an illustrated plate,” or name all five. Separately, the “fourteen screenshots” assertions in [FeaturesPage.tsx:26](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/FeaturesPage.tsx:26) and [SiteBits.tsx:230](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/SiteBits.tsx:230) are also wrong: both the old diff and the new page contain 13 image components. “Nine more ways in” does correctly have nine tiles.

6. **High — reduced motion does not neutralise the scroll-driven animations.** [tailwind.css:126](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/tailwind.css:126) changes `animation-duration`, but time durations on scroll-driven timelines are treated as `auto`; scrolling still drives the tilt, reveal, and nav animations. This is explicitly the behavior defined by [CSS Animations Level 2](https://drafts.csswg.org/css-animations-2/#animation-duration). Add a reduced-motion block mirroring the print override: set `animation: none !important`, `opacity: 1`, and `transform: none` on `.site-reveal`, `.site-tilt`, and `.site-nav`.

7. **Medium — the landing-page heading hierarchy jumps from `h1` to `h3`.** `Showcase` always emits an `h3` at [SiteBits.tsx:192](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/SiteBits.tsx:192), but the three landing showcases begin at [LandingPage.tsx:168](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/LandingPage.tsx:168) before any `h2`. “Who it’s for” and “And deliberately not” are merely paragraphs, so they do not repair the outline. Let `Showcase` accept a heading level, using `h2` on `/` and `h3` below the real `H2`s on `/features`; make the two panel titles actual `h2`s.

8. **Medium — the gradient-heading fallback is not real, and print can lose the headings.** At [styles.css:13380](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/styles.css:13380), `color` does not rescue text once `-webkit-text-fill-color: transparent` applies. A browser that accepts text fill but not clipping, forced-color handling that removes backgrounds, or ordinary printing with background graphics disabled can produce transparent text. The print override at [styles.css:13527](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/styles.css:13527) does not cover this. Put the gradient, clipping, and transparent fill inside a matching `@supports`; under print and forced colors, reset the background and set `-webkit-text-fill-color: currentColor`.

9. **Medium — the no-preflight trap was hit throughout the new components.** Examples include the `Showcase` heading and paragraph at [SiteBits.tsx:190](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/SiteBits.tsx:190) and tile children at [SiteBits.tsx:269](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/SiteBits.tsx:269). `tw:mb-3` removes only one UA margin; the `h3` keeps its default top margin, and several paragraphs keep both default margins. Thus the declared gaps and panel padding are not the rendered rhythm and contribute to the excess height. Add a scoped reset such as `.site :where(h1, h2, h3, p, figure) { margin: 0; }`, then rely on the explicit spacing.

10. **Medium — Diagram is grouped under “Find out what you kept.”** [FeaturesPage.tsx:190](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/FeaturesPage.tsx:190) puts the diagram portrait beside Remember and Quiz under the “Internalise” heading. Diagram is an orienting structural map and previously lived under “The landscape”; the file header’s claim that the order is unchanged is therefore false. Move it back under the landscape section or give diagrams their own section.

11. **Low — the hero border’s clipping and fallback are both unsafe.** `.site-frame` clips overflow at [styles.css:13439](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/styles.css:13439), while the one-pixel masked ring starts at `inset: -1px` at [styles.css:13454](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/styles.css:13454), placing the ring on the clipped side of the padding-box edge. Use `inset: 0`. Also, if neither mask-composite form is supported, the pseudo-element becomes a full gradient wash over the screenshot. Create the pseudo-element only inside a mask-support query.

12. **Low — several newly written sentences violate the page’s provenance rule.** “Nine more ways in” at [LandingPage.tsx:210](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/LandingPage.tsx:210), “The promises underneath” at [LandingPage.tsx:274](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/LandingPage.tsx:274), and “Simple, and reading is never gated” at [LandingPage.tsx:310](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/LandingPage.tsx:310) and [FeaturesPage.tsx:253](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/FeaturesPage.tsx:253) have no source or `[tissue]` marker beside them. Add the relevant product-fact/Greg citation or mark them as tissue.

13. **Low — the supplied “whole change” diff is stale.** [site-redesign.diff](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/output/site-redesign.diff) omits current changes in [SiteBits.tsx:63](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/SiteBits.tsx:63) and [SiteBits.tsx:187](/home/greg/code/spideryarn2/.claude/worktrees/homepage-design-260903/src/web/SiteBits.tsx:187): the mobile nowrap/shrink behavior, hidden Beta badge, and removal of right-aligned showcase copy. Regenerate the scoped diff after the source stops moving.

## Categories with no additional findings

- All new no-fallback custom properties exist; the token test passes.
- The colour mixing uses `oklab`, brand orange is not confused with `--accent`, and contrast is sound: muted body text ≈5.66:1, orange eyebrow ≈7.29:1, and CTA ink on orange ≈7.16:1.
- `backdrop-filter` degrades acceptably because the nav retains an 82%-opaque mixed background.
- The nav has no clipping or transforming ancestor; the hero’s overflow container is its sibling. The 80px sign-in scroll margin clears the 56px bar.
- Unsupported scroll-timeline browsers get visible, flat content. The weak guards should nevertheless test the exact timeline and `animation-range` syntax to protect partial implementations.
- `Frame`’s `hero` and `eager` props both have effects; `Tile.span` matches the scoped modifier selectors. No `Split`, `.site-split`, or `.site-bleed` remnants remain, and every `SiteBits` export has a call site.
- The only dead-code defect found is the `Feature` import above.

## What I would have done differently

I would keep opacity and legibility unconditional and animate a decorative child rather than the semantic content container itself. I would also make heading level explicit in `Showcase`; visual reuse should not hard-code document structure.

Verification: the two targeted test files pass, 50 tests total. The client build and web typecheck fail for findings 1 and 2.