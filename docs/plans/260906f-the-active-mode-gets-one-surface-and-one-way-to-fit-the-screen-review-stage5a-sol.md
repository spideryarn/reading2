Verdict: no 5b code has crept into 5a, and the checkbox remains visibly unticked. But I would not accept 5a as complete yet. The rule doc rhetorically treats the unfinished hover-card behavior as intentional, F46’s browser evidence is missing, and Chat still has real focus-lifecycle defects.

| Severity | What is wrong | What I would do |
|---|---|---|
| **High** | `ChatDialog`’s render-time opener capture fails when another surface hands off to Chat. During Comment → Chat, [ChatDialog.tsx:358](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/ChatDialog.tsx:358) captures the focused control inside the outgoing `CommentDialog`. Only afterward does Comment’s cleanup restore its real opener. When Chat eventually closes, its captured control is disconnected and has no row fallback, so focus lands on `<body>`. Both “Open the conversation” and Discuss routes do this in [Reader.tsx:2175](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/reader/Reader.tsx:2175). | Carry an explicit logical return target through the handoff rather than sampling `activeElement` during render. Add an integration test that opens Comment, hands off to Chat, closes Chat, and checks the original comment control/dock fallback. |
| **High** | The Chrome traversal test measures the current static markup accurately, but it is not a regression test for the real components’ behavior. `renderToStaticMarkup` strips React handlers and runs no effects, while the test supplies `showModal()` itself at [tab-traversal-in-chrome.test.ts:121](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/tests/tab-traversal-in-chrome.test.ts:121). Adding an `onKeyDown` or effect-installed Tab trap to `AnnotateDialog` would leave this test green. It also never presses Shift-Tab. | Mount or hydrate the actual components in Chrome. Exercise Tab and Shift-Tab, require at least one internal control to be reached, and require neither outside sentinel to be reached for the modal. A jsdom companion can cheaply assert that modeless Tab events are not intercepted. |
| **High** | [keyboard.md:367](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/project/keyboard.md:367) quietly turns 5b’s defect into the declared contract: modeless Tab “walks straight through, on purpose”, then [line 401](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/docs/project/keyboard.md:401) says hover cards “owe nothing”. That directly contradicts Stage 5b’s requirement that their controls become reachable. The restore rule also says the universal test is `body/null`, contradicting Chat’s necessary pre-detach containment check. | Keep modal/modeless as the first split, but add modeless subcontracts: passive non-interactive surface; interactive surface whose controls enter logical tab order; focus-taking panel. Say: 12 are intentionally untrapped, 10 currently have correct traversal, and 2 hover cards remain defective. W3C likewise treats a modeless dialog’s controls as part of the sequential order after its trigger, not as content Tab should skip ([W3C Focus Order](https://www.w3.org/WAI/WCAG21/Understanding/focus-order.html)). |
| **Medium** | StrictMode breaks the ordinary draft focus. The initializer being invoked twice is harmless—both calls occur before commit—but the restore cleanup at [ChatDialog.tsx:403](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/ChatDialog.tsx:403) runs during StrictMode’s synthetic setup/cleanup/setup cycle. It restores the opener while the dialog remains mounted. The composer’s second setup does not refocus because its shared nonce ref was already advanced. The real app is wrapped in StrictMode at [main.tsx:207](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/main.tsx:207). | Add a StrictMode test asserting the draft composer remains focused after mount. Make restoration distinguish a genuine unmount from the simulated cycle, or make the second setup re-establish the intended open focus. `[opened]` itself is the right stable dependency. |
| **Medium** | The keyboard Help route opens a Chat draft with no composer. The Help button has already unmounted, and because `openedAs === "draft"`, the Close-focus effect does nothing. When the auto-send turns it into a thread, `openedAs` remains draft, so it still focuses nothing. The reader sits on `<body>` while the panel is open. | Give this route an explicit destination. Given Help’s “ask and keep reading” intent, the paragraph’s `.blk-more` is more coherent than stealing focus into the panel; at minimum, body must not be the accidental answer. |
| **Medium** | F46 was not discharged. The plan required observing Floating UI’s two conditional restore cases in Chrome. The new tests only prove dismissal/containment in jsdom; [profile-panel.test.tsx:223](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/tests/profile-panel.test.tsx:223) dispatches directly on `body`, where no real pointer default moves focus, and neither file asserts `activeElement`. | Add the two browser observations: blank outside press returns to trigger; pressing another real control leaves focus on that control. Until then, keep 5a “in progress”, not “built”. |
| **Medium** | The Referee behavior works today, but its production wiring is unprotected. `buttonRef` is optional at [RefereeCard.tsx:172](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/RefereeCard.tsx:172), and the test’s private `Probe` supplies it independently. Deleting `buttonRef={how.buttonRef}` from [RefereeMode.tsx:212](/home/greg/code/spideryarn2/.claude/worktrees/a5-mode-surface/src/web/modes/referee/RefereeMode.tsx:212) leaves the focus tests green while production loses restoration. | Make `buttonRef` required—there are only two call sites and both already supply it—or mount the real `RefereeBand` in the behavior test. This is the cleanest uncaught mutation. |
| **Medium** | Recording the draft → thread caret loss instead of fixing it was the wrong decision, not merely an under-argued one. If the outgoing composer held focus, moving focus to its semantic replacement preserves an existing interaction; it does not apply the disputed “focus every reopened thread composer” policy. | Record whether the outgoing composer held focus and focus the successor only in that case. Direct `?thread=` opening can continue to target Close. |

The Annotate decision is sound for the current code. Its only application entry is `TableView`’s `onMouseUp`; `selectionchange`, Select All, and keyboard selection do not call `onSelect`. Touch or assistive technology would have to synthesize that same mouse route, and interaction with non-focusable prose has already displaced DOM focus. I would soften “no reader can reach it” to “no current application route preserves an opener”, but I would not build restoration yet.

The Floating UI duplication was also correctly left alone. It is library-internal redundancy; the application should pin the observable inside/outside behavior, not require either private mechanism individually.

Verification: the 187 non-Chrome targeted tests passed, and direct typechecking passed for all projects. The two Chrome tests could not launch in this managed sandbox because Chrome exited on a permissions error, so I cannot independently claim a green traversal run here.