# Findings

No F17+ findings.

The focused composed test could not be rerun in this sandbox because loopback bind was denied with
`EPERM`; the conclusions above are from the exact `f3674e36` blobs, the recorded mutation evidence,
and process/HTTP parser reasoning.
