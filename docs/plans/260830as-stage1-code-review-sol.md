STOP. Stage 1b has one silent-corruption path and three further correctness/observability defects.

## Findings

1. **P0 — A detected shift can be converted into an accepted gap.** `repairShortfall` merges both calls and correctly calls `detectShift`. But that throws `BatchIncomplete` without `shortfall`; the outer catch passes it to `acceptGap`, which discards the repaired labels and checks only the first partial set. If the merged set supplied the twelfth evidence vote, detection fires at 12, then the partial set falls back to 11 and detection abstains. The run publishes eleven shifted labels and one drop. This directly falsifies the comment claiming the removed `LabelsShifted` subtype could not distinguish anything. [src/labels.ts:1507](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:1507), [src/labels.ts:1558](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:1558), [src/labels.ts:1803](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:1803)

2. **P1 — Partial acceptance does not require two shortfalls.** `acceptGap` requires only the first error to carry one. A truncated, refused, malformed, rate-limited, or shifted second response leaves `second` undefined, after which the first gap may still be accepted. The existing test explicitly codifies acceptance after truncation. That is not “the model omitted this twice”; it is one omission followed by no usable answer. Require `again instanceof BatchIncomplete && again.shortfall`. [src/labels.ts:1539](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:1539), [src/labels.ts:1557](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:1557), [tests/labels-shortfall.test.ts:335](/Users/greg/Dropbox/dev/experim/spideryarn2/tests/labels-shortfall.test.ts:335)

3. **P1 — The shift guard is necessarily inert on accepted small batches.** `MIN_SHIFT_EVIDENCE` is 12. A batch of twelve with a surviving shortfall contains at most eleven labels, so `acceptGap` can never detect even a total shift. `planBatches` has no minimum batch size, making this reachable. Refuse partial acceptance when there was insufficient evidence to perform the promised check. [src/labels.ts:202](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:202), [src/labels.ts:1122](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:1122), [src/labels.ts:1583](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:1583)

4. **P1 — The standalone labels command bypasses the article-level 95% backstop.** `generateLabels` exempts every reported drop, then `main` merges and writes without calling `checkCoverage`. Twenty small batches may each spend their one-label floor and the standalone command will publish a tree missing 20%, while `generateToc` would reject it. The advertised backstop therefore depends on which supported entry point ran. [src/labels.ts:1902](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:1902), [src/labels.ts:2054](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:2054)

5. **P1 — `calls` is now a batch count masquerading as a request count.** A successful shortfall repair makes two requests but produces one combined record, and `calls` is `paid.length`. The ToC CLI consequently reports one call, and the cache diagnostic treats zero reads as expected because it believes only one request occurred. This is precisely the guard-that-cannot-fire shape. On accepted non-shortfall failures, the second request’s usage is also absent from the combined record. [src/labels.ts:1247](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:1247), [src/labels.ts:1921](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:1921), [src/toc.ts:1374](/Users/greg/Dropbox/dev/experim/spideryarn2/src/hierarchy.ts:1374)

6. **P2 — `repairedBlocks` double-counts a cascaded boundary.** Cascades deliberately emit one `PartitionRepair` per depth with the same `at` and `size`; summing every entry means one 40-block physical movement through three levels reports 120 blocks. The eval repeats the same arithmetic. `largestRepair` remains sound; the total should deduplicate by `at`, probably taking the maximum size per boundary. [src/toc.ts:690](/Users/greg/Dropbox/dev/experim/spideryarn2/src/hierarchy.ts:690), [src/toc.ts:1328](/Users/greg/Dropbox/dev/experim/spideryarn2/src/hierarchy.ts:1328), [evals/toc-structure/run.ts:494](/Users/greg/Dropbox/dev/experim/spideryarn2/evals/hierarchy-structure/run.ts:494)

7. **P2 — The evidence promised for revisiting `MAX_REPAIRED_BOUNDARIES` cannot be collected.** The first repair is recorded; the second distinct boundary is refused; `buildTree` throws; `generateToc` never returns; therefore the pipeline success log never receives the report. The bound remains a useful safety gate, but its comment’s claimed monitoring path is unreachable exactly when the bound fires. [src/toc.ts:693](/Users/greg/Dropbox/dev/experim/spideryarn2/src/hierarchy.ts:693), [src/pipeline.ts:1457](/Users/greg/Dropbox/dev/experim/spideryarn2/src/pipeline.ts:1457)

8. **P2 — The label eval trusts the producer’s count instead of checking its ids.** It reduces `dropped` to `.length` and subtracts it. A stale or wrong list of the correct length makes the eval report everything accounted for even if the missing ids differ. Compare the actual missing-id set with `labelsFile.dropped`. [evals/toc-labels.ts:252](/Users/greg/Dropbox/dev/experim/spideryarn2/evals/hierarchy-labels.ts:252), [evals/toc-labels.ts:301](/Users/greg/Dropbox/dev/experim/spideryarn2/evals/hierarchy-labels.ts:301)

9. **P2 — A heading can be dropped even though its label is already known.** `onto` deliberately ignores the model’s heading text and uses the block’s text, but only when the ordinal was returned. If that ordinal is omitted twice, partial acceptance leaves the heading bare. Deterministically labelable headings should never consume the drop budget. [src/labels.ts:988](/Users/greg/Dropbox/dev/experim/spideryarn2/src/labels.ts:988)

## Direct answers

Your reading of “allow gaps” is right: snapping them shut preserves reachability. The new code cannot manufacture an empty or backwards range: `spanOf` rejects backwards input and `cursor <= hi` protects the repaired range. It can deliberately assign a large stretch to the semantically wrong section, which `checkTree` cannot detect.

Stage 1a’s functional blast radius looks complete; I found no remaining executable non-fixture fallback and no unnecessary behavioural widening. The comment sweep was incomplete, notably [src/store/artifacts-fs.ts:437](/Users/greg/Dropbox/dev/experim/spideryarn2/src/store/artifacts-fs.ts:437), [src/routes.ts:2073](/Users/greg/Dropbox/dev/experim/spideryarn2/src/routes.ts:2073), and [glossary.md:683](/Users/greg/Dropbox/dev/experim/spideryarn2/docs/project/glossary.md:683). `BuildReport` also still says “off-by-one,” and `repairShortfall` falsely says neither half can reach twelve.