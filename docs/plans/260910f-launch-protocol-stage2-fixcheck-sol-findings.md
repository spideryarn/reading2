F20 — closed: `probeAuth` accepts and installs the pre-run hangup hook (`scripts/run-claude.ts:608-617`), its caller supplies `launch?.hangupBeforeRun` (`scripts/run-claude.ts:813`), and that hook records `not-run` / `hangup` before `runChild` re-raises SIGHUP (`scripts/launch-dir.ts:201-207`; regression `tests/run-claude.test.ts:1022-1071`).
F21 — closed: the last attempt's existing `outFile` is copied to the launch's durable answer target and that target is journalled (`scripts/run-codex.ts:655-675`) before every failure branch (`scripts/run-codex.ts:689-711`); empty, whitespace, and non-zero partial outputs are pinned by `tests/run-codex.test.ts:1203-1235`.
F23 — closed: after the durable start line, `launchStartLines` immediately arms the `EXIT` trap (`scripts/gjd-remote-launch.ts:101-105`); the normal exit writer uses the saved Claude status and then disarms it (`scripts/gjd-remote-launch.ts:113-118`), with the generated job placing those pieces before the guards and directly after status capture (`scripts/gjd-remote.ts:2730-2749`, `scripts/gjd-remote.ts:2800-2803`); guard-failure and no-overwrite regressions are at `tests/gjd-remote-launch-id.test.ts:179-262`.
