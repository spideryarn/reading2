No normal-path `safeOnPass` daemon kill remains: the production callback is synchronous and contained on success, carry, and failure. However, the live tab has multiple P0 wrong-answer defects. H12 also permits two history writers after a daemon lock-loss event.

## Findings

- **H1 — P0 — [routes-usage-history.ts:108](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/routes-usage-history.ts:108)** — A healthy in-flight collection is reported overdue. Health is measured from `collectedAt`, which is captured at pass start, plus 300 seconds. The next record appears only after the scan finishes. Live records show 300,000–300,001 ms source gaps but 13,042–15,693 ms append delays, so during that interval every healthy pass produces “Nothing has been recorded … longer than expected.”

- **H2 — P0 — [UsageHistory.tsx:105](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/web/src/UsageHistory.tsx:105)** — The history never refreshes automatically. Open the tab at 09:00 and leave it open: records appended afterward never appear, `toMs` remains 09:00, and recorder health remains frozen until a manual refresh or remount. The client parses `refreshMs`, but this effect never uses it.

- **H3 — P0 — [usage-history.ts:352](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/usage-history.ts:352), [routes-usage-history.ts:111](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/routes-usage-history.ts:111)** — An omission discards its cadence before recorder-health calculation. If the only/latest record is an oversized-record marker at 10:00 and recording then stops, `lastRecordedAt` becomes 10:00 but `expectedEveryMs` and `overdueByMs` remain `null` forever. Reproduced.

- **H4 — P0 — [routes-usage-history.ts:160](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/routes-usage-history.ts:160)** — Recorder health ignores `predecessor`. If the daemon’s last record is 25 hours old, the 24-hour read returns `samples: []` plus that record as `predecessor`; the route calculates health from `samples` alone and reports that nothing has ever been recorded instead of that recording is more than a day overdue. Reproduced.

- **H5 — P0 — [usage-history.ts:395](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/usage-history.ts:395), [UsageHistory.tsx:136](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/web/src/UsageHistory.tsx:136)** — Unsupported records do not reliably survive positionally or visibly. If a rolled-back reader sees only schema-2 lines after its last old schema-1 predecessor, `firstIn` is `-1`, so all unsupported samples disappear from `samples`. The UI then says “Nothing recorded” and never displays `unsupportedLines`. This violates the seventh evergreen rule.

- **H6 — P0 — [usage-history-series.ts:122](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/web/src/usage-history-series.ts:122)** — Physical unreadable-line holes are carried through the store, route, and client but ignored by the series. Given good points at 10:00 and 10:05 with an unreadable line between them, `view.holes` records the damage but the output has no break, so `WindowLine` connects across bytes this build could not read. Reproduced.

- **H7 — P0 — [UsageHistory.tsx:62](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/web/src/UsageHistory.tsx:62)** — A clock-regression break is rendered as a connected line. For file-order points 10:05 then 10:00, the series creates a cut at 10:05. The renderer’s ascending-time predicate tests `10:05 < 10:00`, gets false, and puts both points in one polyline. The alarm shading appears, but the line still crosses the exact gap the doc says must be broken. Reproduced in rendered SVG.

- **H8 — P0 — [usage-history-series.ts:208](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/web/src/usage-history-series.ts:208), [UsageHistory.tsx:260](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/web/src/UsageHistory.tsx:260)** — Incidents wholly outside the display window are retained and can be shifted into it. For an incident from two hours before the left edge to one hour before it, rendering clamps its negative start to zero but calculates width from the two negative coordinates. The result is a visible bar beginning at the left edge even though no rejection happened during the displayed period; it is also listed under the last 24 hours. Reproduced.

- **H9 — P0 — `usage-history-record.ts:417` and [usage-history-series.ts:208](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/web/src/usage-history-series.ts:208)** — Your double-merge suspicion is correct. The authoritative merger marks equal IDs with conflicting `window` or `resetsAt` unreadable, but it has no production caller. The browser’s second implementation never checks those invariants and publishes the first label with merged counts and span. Reproduced with one ID reported first as `five_hour`, then as `seven_day`. The uncommitted extraction in the workspace appears aimed at this, but the committed revision remains defective.

- **H10 — P0 — [UsageHistory.tsx:127](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/web/src/UsageHistory.tsx:127)** — “Nothing recorded” actually means “no drawable utilisation point.” A collector-failure record, an unknown/unattributed cache, or a pass containing only named unknown windows and rejection incidents makes `hasPoints` false. The page says nothing was recorded while immediately listing facts from records that were recorded. The committed web fixture containing only `nimbus_quill` exercises this state but never rejects the false sentence.

- **H11 — P0 — [usage-history.ts:370](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/usage-history.ts:370)** — A rotation racing a lock-free read loses an entire file from that response. The reader can read old `prev`; the writer then renames current `live` over it and creates a new empty `live`; the reader finishes with old `prev` plus new `live`, omitting the just-rotated history. Health history detects this using the `prev` file’s identity and retries once; usage history omitted that protection. H2 means the false giant gap does not self-heal on the next poll because there is no poll.

- **H12 — P0 — [usage-history-wiring.ts:57](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/usage-history-wiring.ts:57), [daemon.ts:1221](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/overseer/daemon.ts:1221)** — The lock argument holds only at initial opening. The sole predicate is hard-coded `() => true`, and neither opening nor later appends inspect actual ownership. If the daemon detects that another process has taken its main lock while a usage pass is in flight, shutdown deliberately waits for that pass; when it settles, the old daemon still appends while the new daemon may already be writing the same history. Concurrent rotation can overwrite/drop a retained file silently.

- **H13 — P0 — [usage-history-client.ts:200](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/fleet/web/src/usage-history-client.ts:200)** — The browser ignores the route envelope’s `schema`. An old cached client receiving `{schema: 2, kind: "history"}` accepts it as current; if schema 2 moved the sample fields, they are silently dropped and the UI says nothing was recorded. Reproduced with a schema-2 payload. This defeats the version boundary the tolerant parser exists to protect.

- **H14 — P2 — [daemon.ts:355](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/overseer/daemon.ts:355), [daemon.ts:788](/home/greg/code/spideryarn2/.claude/worktrees/usage-limits-tab/tools/overseer/daemon.ts:788)** — `safeOnPass` contains synchronous throws only. TypeScript permits an `async` function where `(outcome) => void` is expected; if such a callback rejects, the `try/catch` has already returned and the rejection can terminate the process. The production callback is synchronous today, so this is not the current live crash path.

The write-side rotation boundary, `0700`/`0600` handling, repair-on-open, and partial-write poisoning behave as specified. A poisoned writer does not un-poison until restart and cannot weld a later record onto a torn tail.

I ran the seven focused committed suites against a temporary archive of exactly `bfb7d729`: **88 tests passed**. I also ran seven targeted reproductions: all demonstrated the outputs described above. `npm run typecheck` did not run; the sandbox denied `tsx`’s IPC socket with `listen EPERM /tmp/tsx-1000/15.pipe`. This was not the previous Vite `.vite-temp` failure.

**fix the listed items**